# Q2 建模思路：实时排队重规划

## 本问作用

Q2 接收 Q1 的已规划路线和游客当前状态，根据 APP 实时排队信息调整剩余路线。

## 模型

锁定已完成项目、当前位置和当前时间，只替换未完成项目的排队参数。实时排队采用基准排队加扰动并随时间回归的形式。

## 求解与验证

比较“保持原剩余路线”和“实时重规划路线”。验证等待节省、得分增益、项目集合变化和已完成项目是否保持不变。

## 代码反向验证与最终思路

代码运行后已核对实际使用的输入、变量、模型、约束、输出表格和图表。当前求解脚本与上述建模思路一致；若后续脚本调整模型、公式、约束或算法，应先更新本文件，再更新论文中的模型表述。

最终思路采用本文档所述模型，并以生成的结果表、中文图和验证输出作为论文依据。`paper/main.tex` 应写入这个代码验证后的最终思路，而不是只写初始设想。
