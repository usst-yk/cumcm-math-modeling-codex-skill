# CUMCM 附录代码格式模板

## 附录 A 核心程序说明

本文核心计算程序位于 `src/` 目录，输出图表分别保存于 `figures/` 和 `tables/` 目录。所有结果均可由代码重新生成。

## A.1 数据读取与预处理

```python
# 说明：读取原始数据，处理缺失值、异常值和单位转换
```

## A.2 模型求解

```python
# 说明：建立模型并求解核心结果
```

## A.3 绘图与表格输出

```python
# 说明：生成论文正文中的图片和表格
```

## 附录要求

- 代码应保留关键注释，说明输入、输出和核心参数。
- 不贴入大量无关调试代码。
- 论文正文中的核心图表必须能由附录代码复现。
- 如代码较长，正文附录可只展示关键片段，并说明完整代码文件名。
